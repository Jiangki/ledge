# Migrating from cvxpy

The canonical migration guide lives at
[`docs/cvxpy_migration.md`](https://github.com/Jiangki/ledge/blob/main/docs/cvxpy_migration.md)
in the repository; every code pair in it is executed and cross-checked
against cvxpy + Clarabel in CI, so the mappings stay correct as APIs
evolve. It is included below verbatim (relative links refer to the
repository tree).

---

{{#include ../../../cvxpy_migration.md}}
